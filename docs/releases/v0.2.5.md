# nonya v0.2.5

실제 Claude/Codex 표면 검증과 자동 업데이트 하드닝 릴리즈입니다.

## Fixes

- macOS 앱이 재시작/재설치/자동 업데이트 뒤에도 마지막 감시 대상을 자동 재개하도록 했습니다.
- GUI 주입 검증이 제출된 메시지 버블을 미전송 draft로 오판해 반복 주입하던 문제를 수정했습니다.
- Claude 데스크톱 앱 주입은 synthetic paste 대신 focused text area `AXValue` 경로를 사용해 중복 입력을 막았습니다.
- 실제 Codex CLI(`cx`)에서 첫 Enter가 prompt를 제출하지 않는 케이스를 확인하고, Codex tmux pane에는 두 번째 Enter를 보냅니다.
- Codex/Claude CLI 실제 세션, tmux cwd 매칭, relaunch/respawn 경로를 다시 검증했습니다.
- Antigravity 프로세스 매핑과 keep-going ledger scrub 비교를 보강했습니다.

## Auto Update

- GitHub 최신 릴리즈 확인, semver 비교, DMG 다운로드, Developer ID 서명 검증, `/Applications/Nonya.app` 교체 및 실패 시 rollback 경로를 추가했습니다.
- 설정에 자동 업데이트 토글과 수동 업데이트 확인 메뉴를 추가했습니다.

## Verification

- `python3 tests/test_audit_fixes.py`
- `python3 tests/test_recovery_engine.py`
- `python3 tests/test_e2e_supervisor.py`
- `bash tests/e2e.sh --live`
- 실제 Claude Code CLI(`claude`) 주입: `CCL_PATCHED_OK`
- 실제 Codex CLI(`codex`) 주입: `CX_PATCHED_OK`
- 실제 Claude 데스크톱 앱 opt-in 주입: 단일 marker 전송 확인
